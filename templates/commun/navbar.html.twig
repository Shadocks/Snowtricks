<div class="container.fluid">
    <nav id="trick-view" class="navbar navbar-inverse">
        <div class="navbar-header navbar-inverse">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('home') }}"><img alt="Brand" src="/favicon.ico"></a>
        </div>
        <div class="collapse navbar-collapse" id="navbar">
            <ul class="nav navbar-nav">
                <li class="active"><a href="{{ path('home') }}">Home</a></li>
                {% if is_granted('ROLE_USER') %}
                    <li><a href="{{ path('trick_add') }}">Ajouter un trick</a></li>
                    <li><a href="{{ path('logout') }}">Déconexion</a></li>
                {% endif %}
                {% if not is_granted('ROLE_USER') %}
                    <li hidden><a href="{{ path('login') }}" hidden>Se connecter</a></li>
                    <li hidden><a href="{{ path('registration') }}" hidden>Créer un compte</a></li>
                {% endif %}
            </ul>
        </div>
    </nav>
</div>
